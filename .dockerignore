# 构建产物（容器里会重新编译，不需要带进去）
gateway
fake_plc
ua_monitor
*.dSYM
build_test_*

# macOS 专用的 snap7 动态库——容器是 Linux，会重新编译 .so，这个用不上
third_party/snap7/lib/

# 杂项
.DS_Store
.claude/
*.o

# 本地测试用的 Python 虚拟环境（macOS 专用，容器里另装 openpyxl）
.venv/
