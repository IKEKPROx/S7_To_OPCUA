# ── 编译产物 / build artifacts ──
gateway
fake_plc
ua_monitor
build_test_*
*.o
*.dSYM/

# 自己编译的 snap7 动态库（平台相关；按 README 附录在本机重新编译）
# self-built snap7 lib (platform-specific; rebuild per README appendix)
third_party/snap7/lib/

# Python 虚拟环境 / Python venv
.venv/

# Docker 导出的镜像归档（大文件）/ exported image archive (large)
*.tar

# 转换脚本生成的配置（示例产物，可重新生成）/ generated configs
config/generated_gateway.json
config/cn_generated.json
config/cn_fake.json

# macOS / 编辑器 / 工具本地文件
.DS_Store
.claude/
.vscode/

# Cowork / 工具会话产物（历史生成物，不入库）/ session outputs
outputs/

# Docker / 构建文件：本地保留(还能 build/推镜像)，但不放上 GitHub —— 仓库只展示代码
.dockerignore
Dockerfile
docker-build.sh
docker-entrypoint.sh
